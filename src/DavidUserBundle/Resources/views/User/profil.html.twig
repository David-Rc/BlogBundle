{% extends 'base.html.twig' %}

{% block navigation %}
    <nav>
        <ul class="navigation">
            <li><a href="{{ path('blog_page_index') }}">Accueil</a></li>
            <li><a href="{{ path('profil') }}">Mon compte</a></li>
            <li><a href="{{ path('logout') }}">Logout</a></li>
        </ul>
    </nav>
{% endblock %}

{% block body %}

    <h1>Hello {{ app.user.username }}</h1>
    <h2>ID : {{ app.user.id }}</h2>
    <button><a href="{{ path('posted_comment', { 'author': app.user.id }) }}">Commentaires postés</a></button>
    <button><a href="{{ path('modify_profil') }}">Modifier le profil</a></button>

{% endblock %}